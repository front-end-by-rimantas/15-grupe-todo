"use strict";function renderList(list){const listPlace=document.querySelector(".container");let HTML="";for(let i=0;i<list.length;i++){const todoItem=list[i];HTML+=`\n            <div class="item">\n                <div class="status ${todoItem.status}"></div>\n                <p class="description">${todoItem.description}</p>\n                <div class="deadline">${todoItem.deadline}</div>\n                <div class="actions">\n                    <div class="action remove">Remove</div>\n                </div>\n            </div>`}return listPlace.innerHTML+=HTML}renderList(todo_list);const removeActions=document.querySelectorAll(".item .action.remove");for(let i=0;i<removeActions.length;i++){const removeElement=removeActions[i];removeElement.addEventListener("click",actionRemoveTodoItem)}function actionRemoveTodoItem(event){const parentItem=event.target.closest(".item");parentItem.remove()}